# Monitoring artifacts (legacy Kubernetes demo stack)

Empty placeholder for any exported Grafana dashboard JSON or Prometheus
alerting rules from the `kube-prometheus-stack` install (see
`../scripts/install-monitoring.sh`). Nothing has been exported here yet.
